# archive-encoder

A verified pipeline for re-encoding a media archive into a compact copy
**without risking the originals and without unmeasured quality loss**.
Battle-tested on a corpus of **754 Samsung Galaxy S24 Ultra videos** (~276 GB,
predominantly 4K VFR): archived at **6.6× smaller with every file either
formally verified or human-reviewed against extracted evidence** — frame counts
exact, audio bit-identical, colors measured.

Everything here is Windows PowerShell 5.1 + ffmpeg. No other dependencies.

## Repository layout

```
archive-encoder/
  archive_encode.ps1   umbrella: runs both flows, one combined summary
  video/               mini-app: tiered video encoding  -> video/README.md
  images/              mini-app: image optimization     -> images/README.md
  core/                the shared engine every flow rides on -- ffmpeg wrappers,
                       VMAF/chroma/pixel-hash verification, encoders, guards
  tests/               sandboxed e2e suites + component harness + negative controls
  assets/              app icon (generated by core\make_icon.ps1)
```

Each mini-app stands alone — its own drop folders, entry scripts, outputs, and
**its own README** — while sharing one copy of the verified engine in `core/`.

## Quickstart

```powershell
# drop videos into video\sources\{important,regular}\
# drop images into images\sources_images\{important,regular}\
.\archive_encode.ps1          # encode everything (add -Parallel for CPU+GPU video tiers)

# then verify before trusting anything:
.\video\confirm_quick.ps1     ;  .\video\confirm_deep.ps1
.\images\confirm_images_quick.ps1  ;  .\images\confirm_images_deep.ps1
```

Or point-and-click: **`video\LaunchUI.bat`**. Full details per flow:
**[video/README.md](video/README.md)** · **[images/README.md](images/README.md)**

## The contract

Whatever flow you use, you end up with:

- an **encoded twin tree** — same filenames, same file dates, mirrored subfolders
- **per-file proof**, not vibes: videos get clean-decode, exact frame count,
  duration, **bit-identical audio hash**, orientation, metadata, VMAF gates
  (mean ≥ 95 / 1%-low ≥ 92, 4K model) and **per-plane chroma SSIM** (VMAF is
  luma-dominant; a pure color defect would otherwise slip by — the negative
  control plants exactly that and the gate catches it). Images get raw-RGBA
  **pixel-hash identity** for lossless conversions, SHA-256 for kept copies,
  SSIM + review pairs for lossy.
- **manifests as receipts** — every decision, ratio, and measurement in CSV
- optional **backup verification** before anything runs, and a closing
  **re-hash proof** that no original changed (`core\verify_backup.ps1` /
  `core\rehash_originals.ps1`)

## Safety invariants (non-negotiable)

- Sources are **read-only** end to end; nothing writes to, renames, or deletes
  an original — ever. Set `ARCHIVE_ENCODER_PROTECTED_ROOT` to your originals
  folder and the engine hard-refuses to write anywhere under it, belt and braces.
- Nothing is ever deleted: superseded outputs are renamed `.bak`, mismatches
  held aside for inspection.
- Encodes write to `.part` and rename only after a frame-count check — an
  interrupted run cannot leave a truncated file that looks finished.
- Every phase gates the next; any FAIL halts with evidence printed.
- Deleting your raws is always **your** manual action, taken after the deep
  confirm's PASS and your own eyes on the review pairs.

## Trust, and how it's maintained

Run these after any change, on any new machine:

```powershell
.\tests\run_tests.ps1        # video e2e: sandboxed drop-folder flow, both tiers,
                             # both confirms, sources-untouched proof (~1 min)
.\tests\run_image_tests.ps1  # image e2e: lossless proof, kept-copy identity, gated lossy
.\tests\smoke_test.ps1       # 41 component checks: probing, 3 encoders, mux, VMAF, guards
.\tests\neg_control.ps1      # plants 5 defects (crushed quality, re-encoded audio,
                             # sideways video, truncation, DESATURATION) and proves
                             # the verifier catches each BY NAME
```

A verifier that has only ever said PASS proves nothing — the negative control
is the evidence the gates can fail things. It has caught real bugs both in the
pipeline and in its own earlier test design.

## Hard-won gotchas (each of these cost real hours)

1. **libvmaf pairs frames by TIMESTAMP, not index — the big one.** On VFR
   phone footage the encode's timestamps drift sub-frame vs the source until
   framesync slips one frame, and every later comparison is against the wrong
   reference. Symptom: VMAF collapses partway through (a perfect file scored
   mean 40 / 1%-low 8), N+1 frame pairs from N-frame inputs, bitrate-independent
   "quality floors". Fix (in `Invoke-Vmaf`): rewrite both sides' PTS to the frame
   ordinal — `settb=1/1000,setpts=N`. Control: source-vs-itself must score 100.00.
2. **PowerShell 5.1 + `$ErrorActionPreference='Stop'` kills scripts when a native
   exe writes to stderr** — and x265/NVENC print banners to stderr on success.
   All native calls must go through `Invoke-FFmpegCapture` / `Invoke-FFprobeJson`.
3. **The null muxer emits a benign "non monotonically increasing dts" warning on
   VFR input** (`-f null -` timebase conversion collides two timestamps). It is not
   a file defect. Never treat "stderr non-empty" as decode failure; judge exit code
   + serious-error patterns. Sources, excerpts, and encodes can all audit clean
   on actual DTS while still triggering it.
4. **ffprobe CSV field order ignores your `-show_entries` order** (`pts` comes
   before `dts` regardless), and values can carry a **trailing comma** when side
   data is present. Query ONE field at a time; parse forgivingly. Misreading this
   manufactured a phantom "520 backward DTS" defect during development.
5. **Windows filter-graph paths need a DOUBLE backslash before the drive colon**
   (`L\\:/dir/file.json`) — single-escape truncates the option value silently and
   VMAF just returns nothing.
6. **`-noautorotate` is NOT a rotation defect** — it keeps the matrix, so the file
   still displays correctly. The real defect is a stripped matrix without
   transposition (`-display_rotation 0`). Verify orientation by comparing effective
   DISPLAY dimensions, which accepts both valid representations.
7. **exFAT mtimes look frozen while a file handle is open** — a `.part` file can
   show "last written 19 minutes ago" while actively growing. Liveness = size
   growth, never mtime. (exFAT also rounds timestamps to 2 s — robocopy re-runs
   may claim files are "modified" that are byte-identical; hashes settle it.)
8. **A filter without an extension matches photos** (a `foo_1*` filter pulls in
   JPGs, which ffprobe happily reports as 1-frame MJPEG "videos").
   `encode_batch.ps1` screens by codec + frame count; still, write filters with
   extensions.
9. **`nb_frames` is container metadata, not truth** — fine on phone mp4s, absent or
   wrong elsewhere. Concrete case: **OBS recordings claim one more frame than the
   stream decodes** (the final packet, cut mid-GOP when recording stops, yields no
   displayable frame — the container says 154, the decoder produces 153, zero
   errors). The frame gates therefore use `nb_frames` as the fast path and, on any
   disagreement, decode the source for real (`Get-DecodedFrameCount`) and judge
   the encode against that count — an encode holding every decodable frame is
   complete. Only mismatching files pay the decode cost.
10. **VMAF's 4K and 1080p models differ by 3–4 points on identical content.**
    Gates only mean something relative to a fixed model (this pipeline: the 4K
    model, everywhere). Changing models silently re-defines your threshold.
11. **WebP cannot carry an ICC profile**, and phones tag photos Display-P3 —
    stripping the profile visibly desaturates in color-managed viewers while
    SSIM stays ~0.99. Caught by human eyes, not metrics; see the image README's
    color-management section for the gamut-mapping fix.

## Requirements

The complete dependency list — everything else used is in-box on Windows 10/11:

| Dependency | Needed for | Notes |
|---|---|---|
| **ffmpeg + ffprobe ≥ 8.0, FULL build** | everything | Must include `libx265` (encoding) **and `libvmaf` (verification)**. ⚠ The "essentials" ffmpeg build **lacks libvmaf** — encodes would work but every quality gate would fail to run. `winget install ffmpeg` installs the full Gyan build, which is correct; verify with `ffmpeg -filters` showing `libvmaf`. Must be on `PATH`. |
| PowerShell 5.1+ | all scripts | In-box. Deliberately built against 5.1's quirks; PowerShell 7 also works. |
| .NET Framework 4.7+ (WPF, System.Drawing) | `video\ui.ps1`, icon generation | In-box on Windows 10/11 — no install. |
| NVIDIA GPU + current driver | fast modes / NVENC only | Optional. The drivers probe for NVENC and fall back to CPU loudly. x265 (CPU) is the archival-quality path and needs no GPU. |

No internet access, no package managers, no Python, no external VMAF model files
(the full ffmpeg build embeds them), nothing installed system-wide. Copy the
folder, have ffmpeg on PATH, run.

---

*All numbers in these READMEs are measured, not guessed: 754-file S24U corpus,
August 2026, reference hardware AMD Ryzen 7 9800X3D + NVIDIA RTX 5080.*
